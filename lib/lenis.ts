'use client';

/**
 * Lenis Smooth Scroll - DISABLED for Performance
 * Laggy scrolling issue - using native scroll instead
 * Keep file for future re-enablement if needed
 */
export function useLenis() {
  // DISABLED: Lenis was causing lag
  // Native browser scrolling is faster and smoother
  return;
}
